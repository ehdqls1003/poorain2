<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kplo.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".AddfriendActivity"></activity>
        <activity android:name=".Recent_RecipeActivity" />
        <activity android:name=".RecipesearchActivity_ty" />
        <activity android:name=".DatasaveActivity" />
        <activity android:name=".RecipesearchActivity" />
        <activity android:name=".RecipehelperActivity" />
        <activity
            android:name=".Recipe"
            android:screenOrientation="landscape" />
        <activity android:name=".ShowrecipeActivity" />
        <activity android:name=".Recipe_rendom" />
        <activity android:name=".MyinfoActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>